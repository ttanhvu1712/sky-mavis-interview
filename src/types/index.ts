export type BalancesInfo = {
  eur: number;
  yen: number;
  usd: number;
};

export type ExchangeRate = {
  eur: number;
  yen: number;
  usd: number;
};

export type UserProfile = {
  userName: string;
  walletAddress: string;
  token: string;
};
